import { Application } from "pixi.js";
//import frameEvent from "@/game/frame-event";

const PixiApp = new Application();
//PixiApp.ticker.add(frameEvent.update.bind(frameEvent));

export default PixiApp;
